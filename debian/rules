#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1
DEBPYTHON_DEFAULT=2.7
DEBPYTHON_SUPPORTED=2.8

export DEBPYTHON_DEFAULT DEBPYTHON_SUPPORTED
%:
	dh $@ --with python2 --buildsystem=python_distutils
